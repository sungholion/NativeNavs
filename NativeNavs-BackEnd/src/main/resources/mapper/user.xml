<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nativenavs.user.mapper.UserMapper">

    <insert id="signUp" parameterType="user">
        INSERT INTO user(email, password, name, nickname, is_nav, birth, user_language, nation, image, phone, created_at, updated_at)
        VALUES (#{email}, #{password}, #{name}, #{nickname}, #{is_nav}, #{birth}, #{user_language}, #{nation}, #{image}, #{phone}, NOW(), NOW())
    </insert>

    <select id="checkDuplicatedEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(email) > 0 FROM user WHERE email = #{email}
    </select>

    <select id="searchOneUser" parameterType="string" resultType="user">
        SELECT * FROM user
        WHERE email = #{email}
    </select>

    <select id="searchAllUser" parameterType="string" resultType="user">
        SELECT * FROM user
    </select>

    <select id="loginSessionWithEmail" parameterType="map" resultType="user">
        SELECT * FROM user WHERE email = #{email} AND password = #{password}
    </select>

    <update id="updateUser" parameterType="map">
        UPDATE user
        <set>
            <if test="updateUser.image != null and updateUser.image != ''">image = #{updateUser.image},</if>
            <if test="updateUser.phone != null and updateUser.phone != ''">phone = #{updateUser.phone},</if>
            <if test="updateUser.nickname != null and updateUser.nickname != ''">nickname = #{updateUser.nickname},</if>
            <if test="updateUser.password != null and updateUser.password != ''">password = #{updateUser.password},</if>
            <if test="updateUser.user_language != null and updateUser.user_language != ''">user_language = #{updateUser.user_language},</if>
        </set>
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user
        where id = #{id}
    </delete>

</mapper>